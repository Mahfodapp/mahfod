import React from 'react';

/**
 * AmbientBackground — intentionally empty.
 * The Mahfod aesthetic relies on pure dark surfaces and sharp neon accents.
 * Blurry background circles are removed; depth is achieved through card
 * elevation, border opacity, and the Islamic pattern overlays.
 */
export function AmbientBackground() {
  return null;
}

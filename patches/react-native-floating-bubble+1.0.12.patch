diff --git a/node_modules/react-native-floating-bubble/android/build.gradle b/node_modules/react-native-floating-bubble/android/build.gradle
index 2173e8d..2e09754 100644
--- a/node_modules/react-native-floating-bubble/android/build.gradle
+++ b/node_modules/react-native-floating-bubble/android/build.gradle
@@ -1,46 +1,21 @@
+apply plugin: 'com.android.library'
 
-buildscript {
-    repositories {
-        jcenter()
-        maven { url 'https://jitpack.io' }
-    }
-
-    dependencies {
-        classpath 'com.android.tools.build:gradle:1.3.1'
-    }
+def safeExtGet(prop, fallback) {
+    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
 }
 
-apply plugin: 'com.android.library'
-
 android {
-    compileSdkVersion 23
-    buildToolsVersion "23.0.1"
+    compileSdkVersion safeExtGet('compileSdkVersion', 34)
+    namespace "com.reactlibrary"
 
     defaultConfig {
-        minSdkVersion 16
-        targetSdkVersion 22
+        minSdkVersion safeExtGet('minSdkVersion', 21)
+        targetSdkVersion safeExtGet('targetSdkVersion', 34)
         versionCode 1
         versionName "1.0"
     }
-    lintOptions {
-        abortOnError false
-    }
-}
-
-repositories {
-    mavenCentral()
-    maven { url 'https://jitpack.io' }
-}
-
-allprojects {
-    repositories {
-        maven { url 'https://jitpack.io' }
-    }
 }
 
 dependencies {
     implementation 'com.facebook.react:react-native:+'
-    //compile 'com.txusballesteros:bubbles:1.2.1',
-    implementation 'com.github.txusballesteros:bubbles-for-android:develop-SNAPSHOT'
 }
\ No newline at end of file
-  
\ No newline at end of file
diff --git a/node_modules/react-native-floating-bubble/android/src/main/AndroidManifest.xml b/node_modules/react-native-floating-bubble/android/src/main/AndroidManifest.xml
index 41a92ab..c7e7078 100644
--- a/node_modules/react-native-floating-bubble/android/src/main/AndroidManifest.xml
+++ b/node_modules/react-native-floating-bubble/android/src/main/AndroidManifest.xml
@@ -1,6 +1,2 @@
-
-<manifest xmlns:android="http://schemas.android.com/apk/res/android"
-          package="com.reactlibrary">
-
+<manifest xmlns:android="http://schemas.android.com/apk/res/android">
 </manifest>
\ No newline at end of file
-  
\ No newline at end of file
diff --git a/node_modules/react-native-floating-bubble/android/src/main/java/com/reactlibrary/RNFloatingBubbleModule.java b/node_modules/react-native-floating-bubble/android/src/main/java/com/reactlibrary/RNFloatingBubbleModule.java
index cde29db..aff6507 100644
--- a/node_modules/react-native-floating-bubble/android/src/main/java/com/reactlibrary/RNFloatingBubbleModule.java
+++ b/node_modules/react-native-floating-bubble/android/src/main/java/com/reactlibrary/RNFloatingBubbleModule.java
@@ -1,171 +1,468 @@
+ package com.reactlibrary;
 
-package com.reactlibrary;
+import android.animation.ValueAnimator;
+import android.animation.AnimatorListenerAdapter;
+import android.animation.Animator;
+import android.app.Activity;
+import android.content.Context;
+import android.content.Intent;
+import android.graphics.Color;
+import android.graphics.PixelFormat;
+import android.net.Uri;
+import android.os.Build;
+import android.os.Handler;
+import android.os.Looper;
+import android.os.VibrationEffect;
+import android.os.Vibrator;
+import android.provider.Settings;
+import android.util.DisplayMetrics;
+import android.view.Gravity;
+import android.view.LayoutInflater;
+import android.view.MotionEvent;
+import android.view.View;
+import android.view.WindowManager;
+import android.view.animation.DecelerateInterpolator;
+import android.widget.FrameLayout;
+import android.widget.TextView;
 
+import com.facebook.react.bridge.Arguments;
+import com.facebook.react.bridge.Promise;
 import com.facebook.react.bridge.ReactApplicationContext;
 import com.facebook.react.bridge.ReactContextBaseJavaModule;
 import com.facebook.react.bridge.ReactMethod;
-import com.facebook.react.bridge.Callback;
-import com.facebook.react.bridge.Promise;
-import com.facebook.react.bridge.Arguments;
 import com.facebook.react.bridge.WritableMap;
 import com.facebook.react.modules.core.DeviceEventManagerModule;
 
-import android.os.Bundle;
-import android.os.Build;
-import android.view.LayoutInflater;
-import android.view.View;
-import android.content.Intent;
-import android.provider.Settings;
-import android.net.Uri;
+public class RNFloatingBubbleModule extends ReactContextBaseJavaModule {
 
-import com.txusballesteros.bubbles.BubbleLayout;
-import com.txusballesteros.bubbles.BubblesManager;
-import com.txusballesteros.bubbles.OnInitializedCallback;
+    private final ReactApplicationContext reactContext;
+    private WindowManager windowManager;
+    private View bubbleView;
+    private View trashView;
+    private WindowManager.LayoutParams bubbleParams;
+    private WindowManager.LayoutParams trashParams;
 
-public class RNFloatingBubbleModule extends ReactContextBaseJavaModule {
+    public RNFloatingBubbleModule(ReactApplicationContext reactContext) {
+        super(reactContext);
+        this.reactContext = reactContext;
+    }
 
-  private BubblesManager bubblesManager;
-  private final ReactApplicationContext reactContext;
-  private BubbleLayout bubbleView;
+    @Override
+    public String getName() {
+        return "RNFloatingBubble";
+    }
 
-  public RNFloatingBubbleModule(ReactApplicationContext reactContext) {
-    super(reactContext);
-    this.reactContext = reactContext;
+    @ReactMethod
+    public void reopenApp() {
+        try {
+            Intent launchIntent = reactContext.getPackageManager().getLaunchIntentForPackage(reactContext.getPackageName());
+            if (launchIntent != null) {
+                reactContext.startActivity(launchIntent);
+            }
+        } catch (Exception e) {
+            // ignore
+        }
+    }
 
-    // try {
-    //   initializeBubblesManager();
-    // } catch (Exception e) {
+    @ReactMethod
+    public void addListener(String eventName) {}
 
-    // }
-  }
+    @ReactMethod
+    public void removeListeners(int count) {}
 
-  @ReactMethod
-  public void reopenApp(){
-    Intent launchIntent = reactContext.getPackageManager().getLaunchIntentForPackage(reactContext.getPackageName());
-    if (launchIntent != null) {
-      reactContext.startActivity(launchIntent);
-    }
-  }
-
-  @Override
-  public String getName() {
-    return "RNFloatingBubble";
-  }
-
-  @ReactMethod // Notates a method that should be exposed to React
-  public void showFloatingBubble(int x, int y, final Promise promise) {
-    try {
-      this.addNewBubble(x, y);
-      promise.resolve("");
-    } catch (Exception e) {
-      promise.reject("");
+    private boolean hasPermission() {
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
+            return Settings.canDrawOverlays(reactContext);
+        }
+        return true;
     }
-  }  
-
-  @ReactMethod // Notates a method that should be exposed to React
-  public void hideFloatingBubble(final Promise promise) {
-    try {
-      this.removeBubble();
-      promise.resolve("");
-    } catch (Exception e) {
-      promise.reject("");
-    }
-  }  
 
-  @ReactMethod // Notates a method that should be exposed to React
-  public void requestPermission(final Promise promise) {
-    try {
-      this.requestPermissionAction(promise);
-    } catch (Exception e) {
+    @ReactMethod
+    public void checkPermission(final Promise promise) {
+        try {
+            promise.resolve(hasPermission());
+        } catch (Exception e) {
+            promise.reject("CHECK_PERMISSION_ERROR", e.getMessage(), e);
+        }
     }
-  }  
-  
-  @ReactMethod // Notates a method that should be exposed to React
-  public void checkPermission(final Promise promise) {
-    try {
-      promise.resolve(hasPermission());
-    } catch (Exception e) {
-      promise.reject("");
+
+    @ReactMethod
+    public void requestPermission(final Promise promise) {
+        try {
+            if (hasPermission()) {
+                promise.resolve("");
+                return;
+            }
+            Activity activity = getCurrentActivity();
+            if (activity != null) {
+                Intent intent = new Intent(
+                    Settings.ACTION_MANAGE_OVERLAY_PERMISSION,
+                    Uri.parse("package:" + reactContext.getPackageName())
+                );
+                activity.startActivity(intent);
+            }
+            promise.resolve("");
+        } catch (Exception e) {
+            promise.reject("PERMISSION_ERROR", e.getMessage(), e);
+        }
     }
-  }  
-  
-  @ReactMethod // Notates a method that should be exposed to React
-  public void initialize(final Promise promise) {
-    try {
-      this.initializeBubblesManager();
-      promise.resolve("");
-    } catch (Exception e) {
-      promise.reject("");
+
+    @ReactMethod
+    public void initialize(final Promise promise) {
+        promise.resolve("");
     }
-  }  
-
-  private void addNewBubble(int x, int y) {
-    this.removeBubble();
-    bubbleView = (BubbleLayout) LayoutInflater.from(reactContext).inflate(R.layout.bubble_layout, null);
-    bubbleView.setOnBubbleRemoveListener(new BubbleLayout.OnBubbleRemoveListener() {
-      @Override
-      public void onBubbleRemoved(BubbleLayout bubble) {
-        bubbleView = null;
-        sendEvent("floating-bubble-remove");
-      }
-    });
-    bubbleView.setOnBubbleClickListener(new BubbleLayout.OnBubbleClickListener() {
-
-      @Override
-      public void onBubbleClick(BubbleLayout bubble) {
-        sendEvent("floating-bubble-press");
-      }
-    });
-    bubbleView.setShouldStickToWall(true);
-    bubblesManager.addBubble(bubbleView, x, y);
-  }
-
-  private boolean hasPermission(){
-    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
-      return Settings.canDrawOverlays(reactContext);
+
+    @ReactMethod
+    public void showFloatingBubble(int x, int y, final Promise promise) {
+        try {
+            if (!hasPermission()) {
+                promise.reject("NO_PERMISSION", "Draw over other app permission not available");
+                return;
+            }
+
+            if (bubbleView != null) {
+                promise.resolve("");
+                return;
+            }
+
+            windowManager = (WindowManager) reactContext.getSystemService(Context.WINDOW_SERVICE);
+            if (windowManager == null) {
+                promise.reject("NO_WINDOW_MANAGER", "Could not get WindowManager service");
+                return;
+            }
+
+            Context context = getCurrentActivity();
+            if (context == null) context = reactContext;
+
+            int layoutFlag;
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+                layoutFlag = WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY;
+            } else {
+                layoutFlag = WindowManager.LayoutParams.TYPE_PHONE;
+            }
+
+            DisplayMetrics metricsForTrash = new DisplayMetrics();
+            windowManager.getDefaultDisplay().getMetrics(metricsForTrash);
+            int screenHeightInit = metricsForTrash.heightPixels;
+            int trashHeightInit = screenHeightInit / 5;
+
+            trashParams = new WindowManager.LayoutParams(
+                WindowManager.LayoutParams.MATCH_PARENT,
+                trashHeightInit,
+                layoutFlag,
+                WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE | WindowManager.LayoutParams.FLAG_NOT_TOUCHABLE,
+                PixelFormat.TRANSLUCENT
+            );
+            trashParams.gravity = Gravity.BOTTOM | Gravity.CENTER_HORIZONTAL;
+            trashView = LayoutInflater.from(context).inflate(R.layout.trash_zone_layout, null);
+            trashView.setAlpha(0.0f);
+            windowManager.addView(trashView, trashParams);
+
+            bubbleParams = new WindowManager.LayoutParams(
+                WindowManager.LayoutParams.WRAP_CONTENT,
+                WindowManager.LayoutParams.WRAP_CONTENT,
+                layoutFlag,
+                WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE,
+                PixelFormat.TRANSLUCENT
+            );
+            bubbleParams.gravity = Gravity.TOP | Gravity.LEFT;
+            bubbleParams.x = x;
+            bubbleParams.y = y;
+
+            bubbleView = LayoutInflater.from(context).inflate(R.layout.bubble_layout, null);
+
+            bubbleView.setOnTouchListener(new View.OnTouchListener() {
+                private int initialX;
+                private int initialY;
+                private float initialTouchX;
+                private float initialTouchY;
+                private boolean isClick;
+
+                private android.os.Handler longPressHandler = new android.os.Handler(android.os.Looper.getMainLooper());
+                private Runnable longPressRunnable = new Runnable() {
+                    @Override
+                    public void run() {
+                        isClick = false;
+                        try {
+                            Vibrator vib = (Vibrator) reactContext.getSystemService(Context.VIBRATOR_SERVICE);
+                            if (vib != null) {
+                                if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+                                    vib.vibrate(VibrationEffect.createOneShot(80, VibrationEffect.DEFAULT_AMPLITUDE));
+                                } else {
+                                    vib.vibrate(80);
+                                }
+                            }
+                        } catch (Exception ignored) {}
+                        sendEvent("floating-bubble-long-press");
+                        if (bubbleView != null) {
+                            bubbleView.animate().scaleX(1.0f).scaleY(1.0f).setDuration(150).start();
+                        }
+                    }
+                };
+
+                @Override
+                public boolean onTouch(View v, MotionEvent event) {
+                    DisplayMetrics metrics = new DisplayMetrics();
+                    windowManager.getDefaultDisplay().getMetrics(metrics);
+                    int screenHeight = metrics.heightPixels;
+                    int trashHeight = screenHeight / 5;
+                    float density = reactContext.getResources().getDisplayMetrics().density;
+                    int dismissThreshold = screenHeight - trashHeight;
+
+                    switch (event.getAction()) {
+                        case MotionEvent.ACTION_DOWN:
+                            longPressHandler.postDelayed(longPressRunnable, 500);
+                            if (bubbleView != null) {
+                                bubbleView.animate().scaleX(0.92f).scaleY(0.92f).setDuration(100).start();
+                            }
+                            initialX = bubbleParams.x;
+                            initialY = bubbleParams.y;
+                            initialTouchX = event.getRawX();
+                            initialTouchY = event.getRawY();
+                            isClick = true;
+                            return true;
+
+                        case MotionEvent.ACTION_UP:
+                            longPressHandler.removeCallbacks(longPressRunnable);
+                            if (bubbleView != null) {
+                                bubbleView.animate().scaleX(1.0f).scaleY(1.0f).setDuration(150).start();
+                            }
+                            float finalY = event.getRawY();
+                            if (finalY > dismissThreshold) {
+                                hideFloatingBubbleInternal();
+                                sendEvent("floating-bubble-remove");
+                            } else {
+                                if (isClick) {
+                                    sendEvent("floating-bubble-press");
+                                }
+                                if (trashView != null) trashView.setAlpha(0.0f);
+                                if (bubbleView != null) bubbleView.setAlpha(1.0f);
+
+                                DisplayMetrics localMetrics = new DisplayMetrics();
+                                windowManager.getDefaultDisplay().getMetrics(localMetrics);
+                                int screenWidth = localMetrics.widthPixels;
+                                int bubbleWidth = bubbleView.getWidth();
+                                int bubbleCenter = bubbleParams.x + (bubbleWidth / 2);
+                                
+                                int targetX = (bubbleCenter < screenWidth / 2) ? 0 : screenWidth - bubbleWidth;
+
+                                ValueAnimator animator = ValueAnimator.ofInt(bubbleParams.x, targetX);
+                                animator.setDuration(250);
+                                animator.setInterpolator(new DecelerateInterpolator());
+                                animator.addUpdateListener(new ValueAnimator.AnimatorUpdateListener() {
+                                    @Override
+                                    public void onAnimationUpdate(ValueAnimator animation) {
+                                        bubbleParams.x = (int) animation.getAnimatedValue();
+                                        if (windowManager != null && bubbleView != null) {
+                                            windowManager.updateViewLayout(bubbleView, bubbleParams);
+                                        }
+                                    }
+                                });
+                                animator.start();
+                            }
+                            return true;
+
+                        case MotionEvent.ACTION_MOVE:
+                            float moveX = event.getRawX() - initialTouchX;
+                            float moveY = event.getRawY() - initialTouchY;
+
+                            if (Math.abs(moveX) > 10 || Math.abs(moveY) > 10) {
+                                isClick = false;
+                                longPressHandler.removeCallbacks(longPressRunnable);
+                            }
+
+                            bubbleParams.x = initialX + (int) moveX;
+                            bubbleParams.y = initialY + (int) moveY;
+                            
+                            float currentY = event.getRawY();
+                            if (currentY > dismissThreshold - (150 * density)) {
+                                float rawRatio = (currentY - (dismissThreshold - (150 * density))) / (150 * density + 100 * density);
+                                float ratio = Math.min(1.0f, Math.max(0.0f, rawRatio));
+                                
+                                if (bubbleView != null) bubbleView.setAlpha(1.0f - (ratio * 0.8f));
+                                if (trashView != null) trashView.setAlpha(ratio);
+                            } else {
+                                if (bubbleView != null) bubbleView.setAlpha(1.0f);
+                                if (trashView != null) trashView.setAlpha(0.0f);
+                            }
+
+                            if (windowManager != null && bubbleView != null) {
+                                windowManager.updateViewLayout(bubbleView, bubbleParams);
+                            }
+                            return true;
+                    }
+                    return false;
+                }
+            });
+
+            windowManager.addView(bubbleView, bubbleParams);
+            promise.resolve("");
+        } catch (Exception e) {
+            promise.reject("SHOW_BUBBLE_ERROR", e.getMessage(), e);
+        }
     }
-    return true;
-  }
 
-  private void removeBubble() {
-    if(bubbleView != null){
-      try{
-        bubblesManager.removeBubble(bubbleView);
-      } catch(Exception e){
+    @ReactMethod
+    public void triggerLastRepSequence(final Promise promise) {
+        final Handler mainHandler = new Handler(Looper.getMainLooper());
+        mainHandler.post(new Runnable() {
+            @Override
+            public void run() {
+                try {
+                    try {
+                        Vibrator vib = (Vibrator) reactContext.getSystemService(Context.VIBRATOR_SERVICE);
+                        if (vib != null) {
+                            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+                                long[] timings = {0, 80, 60, 120, 60, 300};
+                                int[] amplitudes = {0, 80, 0, 150, 0, 255};
+                                vib.vibrate(VibrationEffect.createWaveform(timings, amplitudes, -1));
+                            } else {
+                                long[] pattern = {0, 80, 60, 120, 60, 300};
+                                vib.vibrate(pattern, -1);
+                            }
+                        }
+                    } catch (Exception ignored) {}
+
+                    if (bubbleView == null || windowManager == null) {
+                        if (promise != null) promise.resolve("");
+                        return;
+                    }
+
+                    final TextView waveView = new TextView(reactContext);
+                    waveView.setText("\uD83D\uDC4B");
+                    waveView.setTextSize(28);
+                    waveView.setGravity(Gravity.CENTER);
+
+                    try {
+                        FrameLayout bubbleFrame = (FrameLayout) bubbleView;
+                        FrameLayout.LayoutParams waveParams = new FrameLayout.LayoutParams(
+                            FrameLayout.LayoutParams.MATCH_PARENT,
+                            FrameLayout.LayoutParams.MATCH_PARENT
+                        );
+                        waveParams.gravity = Gravity.CENTER;
+                        bubbleFrame.addView(waveView, waveParams);
+
+                        waveView.setScaleX(0f);
+                        waveView.setScaleY(0f);
+                        waveView.animate().scaleX(1.2f).scaleY(1.2f).setDuration(200).withEndAction(new Runnable() {
+                            @Override
+                            public void run() {
+                                if (waveView.getParent() != null) {
+                                    waveView.animate().rotation(20f).setDuration(150).withEndAction(new Runnable() {
+                                        @Override
+                                        public void run() {
+                                            if (waveView.getParent() != null)
+                                                waveView.animate().rotation(-20f).setDuration(150).withEndAction(new Runnable() {
+                                                    @Override public void run() {
+                                                        if (waveView.getParent() != null)
+                                                            waveView.animate().rotation(10f).setDuration(100).start();
+                                                    }
+                                                }).start();
+                                        }
+                                    }).start();
+                                }
+                            }
+                        }).start();
+                    } catch (Exception ignored) {}
+
+                    mainHandler.postDelayed(new Runnable() {
+                        @Override
+                        public void run() {
+                            if (bubbleView == null || windowManager == null) return;
 
-      }
+                            if (trashView != null) {
+                                trashView.animate().alpha(1.0f).setDuration(300).start();
+                            }
+
+                            DisplayMetrics metrics = new DisplayMetrics();
+                            windowManager.getDefaultDisplay().getMetrics(metrics);
+                            final int screenHeight = metrics.heightPixels;
+                            final int startY = bubbleParams.y;
+                            final int targetY = screenHeight + 100;
+
+                            ValueAnimator dropAnim = ValueAnimator.ofInt(startY, targetY);
+                            dropAnim.setDuration(500);
+                            dropAnim.setInterpolator(new DecelerateInterpolator());
+                            dropAnim.addUpdateListener(new ValueAnimator.AnimatorUpdateListener() {
+                                @Override
+                                public void onAnimationUpdate(ValueAnimator animation) {
+                                    if (bubbleView == null || windowManager == null) return;
+                                    bubbleParams.y = (int) animation.getAnimatedValue();
+                                    float progress = (float)(bubbleParams.y - startY) / (targetY - startY);
+                                    bubbleView.setAlpha(Math.max(0f, 1f - progress));
+                                    try { windowManager.updateViewLayout(bubbleView, bubbleParams); } catch (Exception ignored) {}
+                                }
+                            });
+                            dropAnim.addListener(new AnimatorListenerAdapter() {
+                                @Override
+                                public void onAnimationEnd(Animator animation) {
+                                    mainHandler.post(new Runnable() {
+                                        @Override
+                                        public void run() {
+                                            hideFloatingBubbleInternal();
+                                            sendEvent("floating-bubble-remove");
+                                        }
+                                    });
+                                }
+                            });
+                            dropAnim.start();
+                        }
+                    }, 900);
+
+                    if (promise != null) promise.resolve("");
+                } catch (Exception e) {
+                    if (promise != null) promise.reject("LAST_REP_ERROR", e.getMessage(), e);
+                }
+            }
+        });
     }
-  }
 
+    @ReactMethod
+    public void setBubbleText(String text, final Promise promise) {
+        try {
+            if (bubbleView != null) {
+                TextView textView = bubbleView.findViewById(R.id.bubble_text);
+                if (textView != null) {
+                    textView.setText(text);
+                }
+            }
+            promise.resolve(""); 
+        } catch (Exception e) {
+            promise.reject("SET_TEXT_ERROR", e.getMessage(), e);
+        }
+    }
 
-  public void requestPermissionAction(final Promise promise) {
-    if (!hasPermission()) {
-      Intent intent = new Intent(Settings.ACTION_MANAGE_OVERLAY_PERMISSION, Uri.parse("package:" + reactContext.getPackageName()));
-      Bundle bundle = new Bundle();
-      reactContext.startActivityForResult(intent, 0, bundle);
+    @ReactMethod
+    public void hideFloatingBubble(final Promise promise) {
+        try {
+            hideFloatingBubbleInternal();
+            promise.resolve("");
+        } catch (Exception e) {
+            promise.reject("HIDE_BUBBLE_ERROR", e.getMessage(), e);
+        }
     }
-    if (hasPermission()) {
-      promise.resolve("");
-    } else {
-      promise.reject("");
+
+    private void hideFloatingBubbleInternal() {
+        if (windowManager != null) {
+            if (bubbleView != null) {
+                windowManager.removeView(bubbleView);
+                bubbleView = null;
+            }
+            if (trashView != null) {
+                windowManager.removeView(trashView);
+                trashView = null;
+            }
+        }
+    }
+
+    private void sendEvent(String eventName) {
+        try {
+            WritableMap payload = Arguments.createMap();
+            reactContext
+                .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class)
+                .emit(eventName, payload);
+        } catch (Exception e) {
+            // ignore
+        }
     }
-  }
-
-  private void initializeBubblesManager() {
-    bubblesManager = new BubblesManager.Builder(reactContext).setTrashLayout(R.layout.bubble_trash_layout)
-        .setInitializationCallback(new OnInitializedCallback() {
-          @Override
-          public void onInitialized() {
-            // addNewBubble();
-          }
-        }).build();
-    bubblesManager.initialize();
-  }
-
-  private void sendEvent(String eventName) {
-    WritableMap params = Arguments.createMap();
-    reactContext
-      .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class)
-      .emit(eventName, params);
-  }
 }
\ No newline at end of file
diff --git a/node_modules/react-native-floating-bubble/android/src/main/res/drawable/bubble_background.xml b/node_modules/react-native-floating-bubble/android/src/main/res/drawable/bubble_background.xml
new file mode 100644
index 0000000..0000001
--- /dev/null
+++ b/node_modules/react-native-floating-bubble/android/src/main/res/drawable/bubble_background.xml
@@ -0,0 +1,7 @@
+<?xml version="1.0" encoding="utf-8"?>
+<shape xmlns:android="http://schemas.android.com/apk/res/android"
+    android:shape="oval">
+    <solid android:color="#1A1C21" />
+    <stroke android:width="2dp" android:color="#CCFF00" />
+    <size android:width="72dp" android:height="72dp" />
+</shape>
diff --git a/node_modules/react-native-floating-bubble/android/src/main/res/drawable/trash_zone_background.xml b/node_modules/react-native-floating-bubble/android/src/main/res/drawable/trash_zone_background.xml
new file mode 100644
index 0000000..0000002
--- /dev/null
+++ b/node_modules/react-native-floating-bubble/android/src/main/res/drawable/trash_zone_background.xml
@@ -0,0 +1,9 @@
+<?xml version="1.0" encoding="utf-8"?>
+<shape xmlns:android="http://schemas.android.com/apk/res/android"
+    android:shape="rectangle">
+    <gradient
+        android:angle="90"
+        android:startColor="#00000000"
+        android:endColor="#CC0D0E10"
+        android:type="linear" />
+</shape>
diff --git a/node_modules/react-native-floating-bubble/android/src/main/res/layout/bubble_layout.xml b/node_modules/react-native-floating-bubble/android/src/main/res/layout/bubble_layout.xml
index d2a8762..branded 100644
--- a/node_modules/react-native-floating-bubble/android/src/main/res/layout/bubble_layout.xml
+++ b/node_modules/react-native-floating-bubble/android/src/main/res/layout/bubble_layout.xml
@@ -1,17 +1,23 @@
 <?xml version="1.0" encoding="utf-8"?>
-<com.txusballesteros.bubbles.BubbleLayout
+<FrameLayout
     xmlns:android="http://schemas.android.com/apk/res/android"
-    android:layout_width="wrap_content"
-    android:layout_height="wrap_content"
+    android:layout_width="72dp"
+    android:layout_height="72dp"
     android:clipChildren="false"
-    android:clipToPadding="false">
-
-    <ImageView
-        android:id="@+id/avatar"
-        android:layout_width="70dp"
-        android:layout_height="70dp"
-        android:layout_gravity="center"
-        android:background="@drawable/profile_decorator"
-        android:src="@drawable/bubble_icon"
-        android:scaleType="centerCrop"/>
-</com.txusballesteros.bubbles.BubbleLayout>
\ No newline at end of file
+    android:clipToPadding="false"
+    android:background="@drawable/bubble_background"
+    android:elevation="12dp">
+
+    <TextView
+        android:id="@+id/bubble_text"
+        android:layout_width="wrap_content"
+        android:layout_height="wrap_content"
+        android:layout_gravity="center"
+        android:textColor="#CCFF00"
+        android:textSize="16sp"
+        android:textStyle="bold"
+        android:letterSpacing="0.05"
+        android:fontFamily="sans-serif-medium"
+        android:text="0/5" />
+
+</FrameLayout>
diff --git a/node_modules/react-native-floating-bubble/android/src/main/res/layout/trash_zone_layout.xml b/node_modules/react-native-floating-bubble/android/src/main/res/layout/trash_zone_layout.xml
index 2d73bad..darkmode 100644
--- a/node_modules/react-native-floating-bubble/android/src/main/res/layout/trash_zone_layout.xml
+++ b/node_modules/react-native-floating-bubble/android/src/main/res/layout/trash_zone_layout.xml
@@ -1,20 +1,23 @@
 <?xml version="1.0" encoding="utf-8"?>
 <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
     android:layout_width="match_parent"
     android:layout_height="match_parent">
 
     <View
         android:layout_width="match_parent"
-        android:layout_height="120dp"
+        android:layout_height="140dp"
         android:layout_gravity="bottom"
-        android:background="#3332CD32" />
-
-    <ImageView
-        android:layout_width="40dp"
-        android:layout_height="40dp"
+        android:background="@drawable/trash_zone_background" />
+
+    <TextView
+        android:layout_width="wrap_content"
+        android:layout_height="wrap_content"
         android:layout_gravity="bottom|center_horizontal"
-        android:layout_marginBottom="30dp"
-        android:src="@drawable/bubble_icon"
-        android:tint="#FFFFFF" />
-        
+        android:layout_marginBottom="24dp"
+        android:text="✕"
+        android:textColor="#FF6B6B"
+        android:textSize="28sp"
+        android:textStyle="bold"
+        android:fontFamily="sans-serif-medium" />
+
 </FrameLayout>
diff --git a/node_modules/react-native-floating-bubble/index.js b/node_modules/react-native-floating-bubble/index.js
index b5eb113..e85eecc 100644
--- a/node_modules/react-native-floating-bubble/index.js
+++ b/node_modules/react-native-floating-bubble/index.js
@@ -1,13 +1,14 @@
-
 import { NativeModules } from 'react-native';
 
 const { RNFloatingBubble } = NativeModules;
 
-export const reopenApp = () => RNFloatingBubble.reopenApp();
-export const showFloatingBubble = (x = 50, y = 100) => RNFloatingBubble.showFloatingBubble(x, y);
+export const showFloatingBubble = (x = 50, y = 50) => RNFloatingBubble.showFloatingBubble(x, y);
 export const hideFloatingBubble = () => RNFloatingBubble.hideFloatingBubble();
+export const reopenApp = () => RNFloatingBubble.reopenApp();
 export const checkPermission = () => RNFloatingBubble.checkPermission();
 export const requestPermission = () => RNFloatingBubble.requestPermission();
 export const initialize = () => RNFloatingBubble.initialize();
+export const setBubbleText = (text) => RNFloatingBubble.setBubbleText(text);
+export const triggerLastRepSequence = () => RNFloatingBubble.triggerLastRepSequence();
 
-export default { showFloatingBubble, hideFloatingBubble, requestPermission, checkPermission, initialize, reopenApp };
+export default { showFloatingBubble, hideFloatingBubble, requestPermission, checkPermission, initialize, reopenApp, setBubbleText, triggerLastRepSequence };
\ No newline at end of file
